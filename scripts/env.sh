# source scripts/env.sh
# Pins the CLI used for every build/deploy in this repo (brief section 6).
export PATH="$HOME/.local/bin/stellar-27:$PATH"
export RUSTUP_TOOLCHAIN=1.91.1
export STELLAR_NETWORK=testnet
